/* boards/nrf7002dk_nrf5340_cpuapp.overlay */


&spi4 { status = "okay"; };      /* SPI bus for the flash */
&mx25r64 { status = "okay"; };   /* The MX25R6435F on SPI4 */

/ {
    aliases { spi-flash0 = &mx25r64; };
    chosen  { nordic,pm-ext-flash = &mx25r64; };  // PM’s external_flash maps here
 };

/* 
&spi4 { status = "okay"; };
&mx25r64 {
    status = "okay";
    partitions {
        compatible = "fixed-partitions";
        #address-cells = <1>;
        #size-cells = <1>;
        lfs0: partition@0 { 
            label = "lfs_ext"; 
            reg = <0x00000000 0x00200000>; 
        };
    };
};

 / {
    fstab {
        compatible = "zephyr,fstab";
        lfs1: lfs1 {
            compatible = "zephyr,fstab,littlefs";
            mount-point = "/lfs";
            partition = <&lfs0>;
            automount;
            read-size = <16>;
            prog-size = <16>;
            cache-size = <64>;
            lookahead-size = <32>;
            block-cycles = <512>;
        };
    };
};
*/